# 博客维护手册

## 目录
- [基础命令](#基础命令)
- [写新文章](#写新文章)
- [编辑旧文章](#编辑旧文章)
- [图片管理](#图片管理)
- [主题外观配置](#主题外观配置)
- [动态特效](#动态特效)
- [页面管理](#页面管理)
- [部署发布](#部署发布)

---

## 基础命令

```bash
npx hexo server          # 本地预览 http://localhost:4000
npx hexo new "标题"       # 创建新文章
npx hexo generate        # 生成静态文件（public/）
npx hexo clean           # 清理缓存
npx hexo clean && npx hexo generate   # 清理后重建
```

---

## 写新文章

```bash
npx hexo new "我的新文章标题"
```

这会在 `source/_posts/` 下生成 `我的新文章标题.md`，同时自动创建一个同名空文件夹用于放图片。

打开 `.md` 文件，修改顶部的 frontmatter：

```yaml
---
title: 我的新文章标题          # 标题
date: 2026-05-06 12:30:00     # 日期
categories:
  - Leetcode                  # 分类（可选多个）
tags:
  - 算法                      # 标签（可选多个）
  - C++
---

在这里写正文...
```

### 分类说明

当前有 5 个分类：`Leetcode`、`学习笔记`、`PwnCollege`、`春招`、`默认知识库`。可以自创新分类，直接写在 `categories:` 下面就行。

---

## 编辑旧文章

直接编辑 `source/_posts/` 下对应的 `.md` 文件，改完 `npx hexo generate` 重新生成即可。

---

## 图片管理

### 新文章贴图

1. 把图片放到 `source/_posts/文章标题/` 文件夹里
2. 在 markdown 中用 asset 标签引用：

```
{% asset_img image-0.png %}
```

不是 `![](image.png)`，是用 `{% asset_img %}` 标签，这样才能正确显示。

### 站点通用图片

放到 `source/images/` 下，引用时用绝对路径：
```
![](/images/my-photo.jpg)
```

---

## 主题外观配置

主题配置文件：`_config.butterfly.yml`

改完刷新 `http://localhost:4000` 即时生效。

### 修改头像

```yaml
avatar:
  img: 你的头像URL或本地路径    # 例如 /images/avatar.png
  effect: true                  # 鼠标悬停旋转效果
```

### 修改博客标题/副标题

编辑 `_config.yml`：

```yaml
title: 博客名
subtitle: '副标题'
description: '描述文字'
author: 你的名字
```

### 修改公告栏

`_config.butterfly.yml` 中：

```yaml
aside:
  card_announcement:
    enable: true
    content: 这里写你的公告内容，支持HTML
```

### 修改 Banner 背景图/色

```yaml
# 首页 banner
index_img: 'https://图片URL'   # 或用渐变色: 'linear-gradient(135deg, #667eea 0%, #764ba2 100%)'

# 归档页
archive_img: '...'

# 标签页
tag_img: '...'

# 分类页
category_img: '...'

# 默认（其他页面）
default_top_img: '...'
```

### 修改导航菜单

```yaml
menu:
  首页: / || fas fa-home
  归档: /archives/ || fas fa-archive
  分类: /categories/ || fas fa-folder-open
  标签: /tags/ || fas fa-tags
  关于: /about/ || fas fa-user
  # 添加新页面：页面名: /路径/ || fontawesome图标名
```

图标名在这里找：https://fontawesome.com/icons

### 侧边栏位置

```yaml
aside:
  position: right   # left 或 right
```

### 首页布局

```yaml
index_layout: 5     # 1-7，尝试不同数字看效果
# 1: 左图右文 2: 右图左文 3: 交错 4: 上图下文
# 5: 文在图上方 6: 瀑布流 7: 瀑布流+文在图上方
```

### 代码块风格

```yaml
code_blocks:
  theme: darker      # darker / pale night / light / ocean
  macStyle: true     # Mac风格圆点
  copy: true         # 复制按钮
```

### 开启/关闭暗色模式

```yaml
darkmode:
  enable: true             # 总开关
  button: true             # 手动切换按钮
  autoChangeMode: 1        # 0=手动 1=跟随系统 2=18-6点自动
```

### 关于页面

编辑 `source/about/index.md` 即可修改关于页内容。

### 404页面

```yaml
error_404:
  enable: true
  subtitle: '页面未找到...'
```

### 页脚设置

```yaml
footer:
  owner:
    enable: true
    since: 2024          # 建站年份
  custom_text: 你的自定义文字
```

---

## 动态特效

所有特效开关都在 `_config.butterfly.yml` 里：

| 特效 | 配置项 | 说明 |
|------|--------|------|
| 彩色丝带背景 | `canvas_ribbon.enable` | 动态线条背景，`click_to_change: true` 点击换色 |
| 鼠标烟花 | `fireworks.enable` | 点击弹出烟花 |
| 点击文字 | `clickShowText.enable` | 点击随机弹出文字/emoji |
| 打字特效 | `activate_power_mode.enable` | 编辑器风格的粒子效果 |
| 首页打字机 | `subtitle.enable` | 首页副标题打字机效果 |
| 页面加载条 | `preloader.enable` | 顶部加载进度条 |
| 页面过渡动画 | `enter_transitions` | 页面切换淡入效果 |
| 圆角UI | `rounded_corners_ui` | 全局圆角卡片 |
| 阅读模式 | `readmode` | 右下角阅读模式按钮 |

关闭某个特效直接把 `enable: true` 改成 `enable: false` 即可。

---

## 页面管理

### 创建新页面

```bash
npx hexo new page "page-name"
```

编辑 `source/page-name/index.md`，内容格式和文章相同。

### 页面添加到导航

在 `_config.butterfly.yml` 的 `menu:` 下添加：
```yaml
menu:
  新页面: /page-name/ || fas fa-star
```

---

## 安装插件

```bash
npm install 插件名 --save
```

常用插件：

```bash
npm install hexo-generator-feed --save       # RSS订阅
npm install hexo-generator-sitemap --save    # sitemap
npm install hexo-abbrlink --save             # 短链接
npm install hexo-filter-nofollow --save      # 外链 nofollow
```

---

## 更新主题

```bash
npm update hexo-theme-butterfly
```

更新后对比 `_config.butterfly.yml` 和新版默认配置，看有没有新功能可加。默认配置在 `node_modules/hexo-theme-butterfly/_config.yml`。

---

## 部署发布

```bash
git add -A
git commit -m "描述改动"
git push
```

推送后 GitHub Actions 自动构建部署，无需手动操作。等待1-2分钟就能在 `https://afogsheep.github.io` 看到更新。

### 检查部署状态

访问 `https://github.com/AFOGSHEEP/AFOGSHEEP.github.io/actions` 查看构建进度。

---

## 目录结构速查

```
├── _config.yml              # Hexo主配置（站点名、URL等）
├── _config.butterfly.yml    # 主题配置（外观、特效等）
├── source/
│   ├── _posts/              # 文章 .md 文件
│   │   ├── 文章名.md
│   │   └── 文章名/          # 文章的图片文件夹
│   ├── images/              # 站点通用图片
│   ├── about/               # 关于页面
│   ├── tags/                # 标签页
│   └── categories/          # 分类页
├── scaffolds/               # 模板（不用管）
├── themes/                  # 主题（不用管）
├── public/                  # 生成的静态文件（不用管，自动生成）
├── node_modules/            # 依赖包（不用管）
├── .github/workflows/       # 自动部署配置
└── 维护手册.md               # 本文件
```
