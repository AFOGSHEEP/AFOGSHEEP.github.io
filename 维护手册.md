# 博客维护手册

## 目录
- [基础命令](#基础命令)
- [写新文章](#写新文章)
- [编辑旧文章](#编辑旧文章)
- [小说/系列类发布](#小说系列类发布)
- [图片管理](#图片管理)
- [主题外观配置](#主题外观配置)
- [页面管理](#页面管理)
- [部署发布](#部署发布)

---

## 基础命令

```bash
npx hexo server          # 本地预览 http://localhost:4000
npx hexo new "标题"       # 创建新文章
npx hexo generate        # 生成静态文件（public/）
npx hexo clean           # 清理缓存
npx hexo clean && npx hexo generate   # 清理后重建
```

---

## 写新文章

```bash
npx hexo new "我的新文章标题"
```

这会在 `source/_posts/` 下生成 `我的新文章标题.md`，同时自动创建一个同名空文件夹用于放图片。

打开 `.md` 文件，修改顶部的 frontmatter：

```yaml
---
title: 我的新文章标题          # 标题
date: 2026-05-06 12:30:00     # 日期
categories:
  - Leetcode                  # 分类（可选多个）
tags:
  - 算法                      # 标签（可选多个）
  - C++
---

在这里写正文...
```

### 分类说明

当前 7 个分类：`学习笔记`、`Leetcode`、`灵异故事`、`默认知识库`、`PwnCollege`、`春招`、`阅读`。可以自创新分类，直接写在 `categories:` 下面就行。

---

## 编辑旧文章

直接编辑 `source/_posts/` 下对应的 `.md` 文件，改完 `npx hexo generate` 重新生成即可。

---

## 小说/系列类发布

灵异故事、小说、连载等内容型系列的发布铁律：

1. **一个系列 = 一篇文章，绝不拆分**。系列内部用小标题分部（如 `## Part 1 · 2015-08-26 发帖 · 得分 7091`），每部保留自己的元信息（日期/得分/原帖链接）。
2. **单篇只配 1 张封面**：front matter 写 `cover:` 字段（见「图片管理 → 文章封面图」）。
3. 封面图统一放 `source/images/stories/`；**图片由文件拖拽提供**（拖到 `~/Pictures` 再取用），不要走聊天粘贴——粘贴临时目录分钟级被系统清理，已因此丢过图。
4. 中英对照成稿存放在 `~/Downloads/Reddit灵异故事/`（01–07 编号），博客文章由 `tools/make_posts.py` 从成稿自动转换生成（AI 维护使用；改了成稿就重跑脚本再推送）。
5. 推送前必须 `npm run build` 本地验证，新分类/坏封面路径在这里暴露。

现状：`灵异故事` 分类 3 篇——微笑男、无尽之屋、森林里的楼梯·搜救官系列（全五部合并版，`tools/make_posts.py` 会把五部合成一篇并自动删除拆分残留）。

---

## 图片管理

### 新文章贴图

1. 把图片放到 `source/_posts/文章标题/` 文件夹里
2. 在 markdown 中用 asset 标签引用：

```
{% asset_img image-0.png %}
```

不是 `![](image.png)`，是用 `{% asset_img %}` 标签，这样才能正确显示。

### 站点通用图片

放到 `source/images/` 下，引用时用绝对路径：
```
![](/images/my-photo.jpg)
```

### 文章封面图

front matter 加 `cover:` 字段，主题 `post_cover` 助手会把它渲染成文章页顶部大图：

```yaml
cover: /images/stories/cover-1.jpg   # 建议写绝对路径
```

封面图统一放 `source/images/stories/`（`helper` 里相对路径会按文章 URL 解析，容易踩坑，所以用 `/` 开头）。

---

## 主题外观配置

当前主题：**fancy-avalanche**（自研，随仓库走）。

- 主题配置：`themes/fancy-avalanche/_config.yml`
- 站点全局配置：`_config.yml`（title/subtitle/url/background 等）
- 样式系统：Tailwind 预编译，`npm run build` = `npm run tw && hexo generate`（源文件 `tailwind.config.js` + `tailwind.input.css`；模板里新增 Tailwind 没见过的 class 会报 MISS，跑一次 `npm run tw` 即可）
- **双风格主题系统**（2026-09 上线）：顶栏「鲸蓝 / 素瓷」芯片切换 + 明暗按钮，全站颜色走 `--fa-*` CSS 变量（与 Tailwind accent 同源），状态存 localStorage（`fa-style` + `theme`）
- 背景：默认纯 CSS `ambient` 模式（mesh 渐变 + 漂移光斑 + 纸纹）；Unsplash 轮播仍在，改 `_config.yml` 的 `background.type` 可切回
- `_config.butterfly.yml` 是旧主题遗留文件，当前主题**不读取**，可忽略（未来可删）；旧 butterfly 特效键（烟花/丝带/打字机等）已随主题更替失效
- 搜索索引：`scripts/trimmed-search.js` 生成 `search.json` 按需加载；拉丁字体自托管于 `source/fonts/`

### 关于页面

编辑 `source/about/index.md` 即可修改关于页内容。

## 页面管理

### 创建新页面

```bash
npx hexo new page "page-name"
```

编辑 `source/page-name/index.md`，内容格式和文章相同。

### 页面添加到导航

在 `_config.butterfly.yml` 的 `menu:` 下添加：
```yaml
menu:
  新页面: /page-name/ || fas fa-star
```

---

## 安装插件

```bash
npm install 插件名 --save
```

常用插件：

```bash
npm install hexo-generator-feed --save       # RSS订阅
npm install hexo-generator-sitemap --save    # sitemap
npm install hexo-abbrlink --save             # 短链接
npm install hexo-filter-nofollow --save      # 外链 nofollow
```

---

## 更新主题

主题在仓库内（`themes/fancy-avalanche/`），随博客一起 git 管理，**没有 npm 更新一说**。改了主题模板/样式：

```bash
npm run tw      # 只有新增/修改了 Tailwind class 才需要
npm run build   # 验证
```

---

## 部署发布

```bash
git add -A
git commit -m "描述改动"
git push origin main
```

推送后 **Cloudflare Pages** 自动构建部署（构建命令 `npm run build`，输出目录 `public/`），1-2 分钟生效。站点地址：`https://afogsheep-github-io.pages.dev`。

> - **不要用 Wrangler/Worker 部署**——Worker 和 Pages 是两个不同产品，本博客用的是 Pages
> - 自定义域名在 Cloudflare Pages 控制台 → Custom domains 配置，仓库里不需要 CNAME
> - `.github/workflows/deploy.yml`（GitHub Pages）是历史遗留，正式站点以 Cloudflare Pages 为准
> - 2026-09 构建变更：`npm run build` = `npm run tw && hexo generate`（tailwind 预编译已并入，本地预览 `npm run server` 自动包含）

### 检查部署状态

Cloudflare Pages 控制台查看构建进度与日志。

---

## 目录结构速查

```
├── _config.yml              # Hexo主配置（站点名、URL等）
├── _config.butterfly.yml    # 旧主题遗留（当前主题不读取，可删）
├── tools/
│   └── make_posts.py        # 灵异故事成稿 → 博客文章转换（AI 用）
├── source/
│   ├── _posts/              # 文章 .md 文件
│   │   ├── 文章名.md
│   │   └── 文章名/          # 文章的图片文件夹
│   ├── images/
│   │   └── stories/         # 文章封面图（front matter cover 字段引用）
│   ├── about/               # 关于页面
│   ├── tags/                # 标签页
│   └── categories/          # 分类页
├── scaffolds/               # 模板（不用管）
├── themes/fancy-avalanche/  # 当前主题（自研，主题配置在其 _config.yml）
├── public/                  # 生成的静态文件（不用管，自动生成）
├── node_modules/            # 依赖包（不用管）
├── .github/workflows/       # deploy.yml 为历史遗留；正式部署 = Cloudflare Pages
└── 维护手册.md               # 本文件
```
